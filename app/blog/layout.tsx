export const metadata = {
  title: 'Blog — Bovée & Thill Business Communication',
  description: 'Expert insights on business communication education.',
};

export default function BlogLayout({ children }: { children: React.ReactNode }) {
  return children;
}
